language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-haxiom-baseline' ]; then cd .. && eval "mv $currentfolder generator-haxiom-baseline" && cd generator-haxiom-baseline; fi

